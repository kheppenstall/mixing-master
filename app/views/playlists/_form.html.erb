<div class="container">
  <div class="col-md-6">
    <%= bootstrap_form_for(@playlist) do |f| %>
      <%= f.text_field :name %>

     
      <%= f.submit nil, :class => 'btn btn-success' %>
    <% end %>
  </div>
</div>