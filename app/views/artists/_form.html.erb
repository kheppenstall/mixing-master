<div class="container">
  <div class="col-md-6">
    <%= bootstrap_form_for(@artist) do |f| %>
      <%= f.text_field :name %>
      <%= f.text_field :image_path %>
      <%= f.submit nil, :class => 'btn btn-success' %>
    <% end %>
  </div>
</div>