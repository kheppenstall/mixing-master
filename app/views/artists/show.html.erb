<h1><%= @artist.name %></h1>

<div class="container">
<%= image_tag @artist.image_path %>
</div>

<div class="container">
  </br>
  <%= link_to "All Artists", artists_path, class:"btn btn-primary"%></br><br>
  <%= link_to "Edit", edit_artist_path(@artist), class:"btn btn-warning"%></br><br>
  <%= link_to "Delete", artist_path(@artist), method: :delete, class:"btn btn-danger", data: {confirm: "Are you sure?"}%>
</div>
