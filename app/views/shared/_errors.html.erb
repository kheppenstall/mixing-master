 
 <% if @errors %>
   <div class="alert alert-danger">
    <h4><%= pluralize(@errors.count, "error") %> prohibited this record from being saved:</h4>
    <ul>
      <% @errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
    </div>
  <% end %>