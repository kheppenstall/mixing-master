<div class="container">
  <div class="col-md-6">
    <%= bootstrap_form_for([@artist, @song]) do |f| %>
      <%= f.text_field :title %>
      <%= f.submit nil, :class => 'btn btn-success' %>
    <% end %>
  </div>
</div>