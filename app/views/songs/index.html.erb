<h2>All Songs</h2>

<div class="container">
  <div class="col-md-2">
    <ul class="list-group">
    <% @songs.each do |song| %>
      <%= link_to song.title, song_path(song), class:"list-group-item" %>
    <% end %>
    </ul>
  </div>
</div>